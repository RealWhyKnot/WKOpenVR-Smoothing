ipch
.vs
*.db
*.opendb
*.aps
*.user
.code
/install/*.exe
[bB]in

# build.ps1 outputs
/release/
/build/

# Downloaded by the release workflow (vcredist for the NSIS installer); never
# committed. Local installer builds without it skip the VC++ redistributable bundle.
/install/vcredist_x64.exe
# NSIS-built installer output lands next to installer.nsi.
/install/FingerSmoothing.exe
/install/FingerSmoothing-*-Setup.exe
# Per-session local-build counter (regenerated each build)
/build/local_build_state.json

# Editor / agent local state
.claude/
CLAUDE.md
.claude.local.json
.anthropic/

# Generated by build.ps1; embeds the per-build version stamp into the overlay
# so the UI footer displays the live build.
src/overlay/BuildStamp.h
version.txt
